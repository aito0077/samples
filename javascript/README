This is an in progress project:
The af library is a wrapper to extend and force the backbone library.
The 'altas' is a kind of wizard to achieve in several steps a final goal.
It uses backbone, underscore, jquery.
 

